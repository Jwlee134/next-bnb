import { NextPage } from "next";
import React from "react";
import RegisterRoom from "src/components/register";

const Geometry: NextPage = () => {
  return <RegisterRoom />;
};

export default Geometry;
